> ## MANDATORY STARTUP
> Before any continuation work, read the repository-root `BRIDGE_PROTOCOL.md` in full.
> Initialize ChatGPT Thinking under that protocol.
> Every instruction sent to Bridge, and every Thinking response after a Bridge result, must remain consistent with it.

# Tre-Up Overview Handoff Entry Point

This repository currently uses this file as the Bridge-aware continuation entry point.

After `BRIDGE_PROTOCOL.md`, inspect only the minimum project sources necessary to establish current truth for the active task. Do not invent a rigid universal startup sequence.

The cross-project role split remains:
- Yiğit = owner / genuine human judgement,
- ChatGPT Thinking = manager / brain,
- Bridge = operator / hands.

If a future canonical project handoff is added elsewhere in this repository, it must begin with the same mandatory `BRIDGE_PROTOCOL.md` pointer.
